Chaque onglet principal dispose de son propre dossier.

Les pages doivent passer par `window.RedacServices` pour accéder aux données, afin de garder les détails de stockage dans `database/` et `repositories/`.
